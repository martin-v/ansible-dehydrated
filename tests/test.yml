---


- hosts: all
  remote_user: root
  tasks:
  - name: Install nginx-light as test server
    apt:
      package: "nginx-light"
      state: present


- hosts: all
  remote_user: root
  vars_files:
    - test_vars.yml
  roles:
    - martin-v.dehydrated
